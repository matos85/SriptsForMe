
# Jupyter Notebook Launcher

## Описание
Этот проект предоставляет простой способ запуска Jupyter Notebook с автоматической проверкой его установки.
 Если Jupyter не установлен, скрипт автоматически установит его перед запуском.
 После запуска Jupyter Notebook, он будет автоматически открыт в вашем браузере. Если браузер закрыть, терминал автоматически закроется.

## Содержимое проекта
- `JNI.py`: Python-скрипт, который запускает Jupyter Notebook.
- `run_jni.bat`: BAT файл для запуска Python-скрипта в командной строке.

## Установка

### Требования
- Python версии 3.x установлен на вашем компьютере.
- Убедитесь, что `pip` установлен и доступен из командной строки.
- Система должна поддерживать запуск Jupyter Notebook (Windows, macOS, Linux).

### Шаги по установке

1. **Скачать проект**
   - Клонируйте репозиторий или скачайте файлы `JNI.py` и `run_jni.bat` в желаемую директорию на вашем компьютере.

2. **Установка Python**
   - Если Python ещё не установлен, скачайте его с [официального сайта Python](https://www.python.org/downloads/) и установите. Во время установки выберите опцию "Add Python to PATH".

3. **Проверка установки Python**
   - Откройте командную строку (нажмите `Win + R`, введите `cmd` и нажмите `Enter`).
   - Введите команду:
     ```bash
     python --version
     ```
   - Убедитесь, что версия Python отображается правильно.

## Использование

1. **Запуск проекта**
   - Дважды щёлкните на файл `run_jni.bat`.
   - Скрипт проверит, установлен ли Jupyter Notebook.
   - Если Jupyter не установлен, он будет автоматически установлен.
   - После этого Jupyter Notebook запустится, и он откроется в браузере.

2. **Закрытие Jupyter Notebook**
   - Закройте браузер, чтобы завершить работу Jupyter Notebook.
   - После закрытия браузера, командное окно тоже закроется автоматически.

## Устранение неполадок
- **Ошибка "python не является внутренней или внешней командой"**:
  - Убедитесь, что Python установлен и его путь добавлен в переменную окружения PATH.
  
- **Jupyter не устанавливается**:
  - Убедитесь, что `pip` установлен и доступен из командной строки. Для обновления pip выполните:
    ```bash
    python -m pip install --upgrade pip
    ```

## Лицензия
Этот проект лицензирован под MIT License. См. файл LICENSE для подробностей.

## Контрибьюция
Любые предложения или улучшения кода приветствуются! Пожалуйста, создайте issue или pull request.

```

### Пояснения к разделам README:

- **Описание** — краткий обзор того, что делает ваш проект.
- **Содержимое проекта** — перечисление файлов проекта с коротким описанием их функций.
- **Установка** — инструкции по установке Python, проверке установки Python, и скачиванию проекта.
- **Использование** — пошаговые указания по запуску проекта и взаимодействию с ним.
- **Устранение неполадок** — возможные проблемы и способы их решения.
- **Лицензия** — информация об лицензии проекта.
- **Контрибьюция** — как другие могут помочь улучшить проект.

Этот шаблон можно адаптировать под конкретные требования вашего проекта и дополнить по мере необходимости.